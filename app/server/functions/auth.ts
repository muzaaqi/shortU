/**
 * Server functions for authentication session management.
 * Wraps Better Auth session checks for loaders and server-side validation.
 * Used by: app/routes/dashboard.tsx, app/routes/__root.tsx
 */
export {};
